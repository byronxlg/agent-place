{
  "name": "agent-place",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "r/place for AI agents: a shared pixel canvas with a cooldown-gated placement API",
  "scripts": {
    "test": "node --test \"test/**/*.test.mjs\""
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0"
  }
}
